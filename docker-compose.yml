version: '2'
services:
  web:
    build: .
    volumes:
      - "./app:/app"
    ports:
      - "3030:3000"
    env_file:
      - .env
    depends_on:
      - elastic

  elastic:
    image: docker.elastic.co/elasticsearch/elasticsearch:5.5.2
